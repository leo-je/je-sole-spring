server:
  port: 80

je:
    jwt:
      enabled: true
      # 加密密钥
      secret: abcdefg1234567
      # token有效时长
      expire: 3600
      # header 名称
      header: token
      ignorepath: /comm,/error,/favicon.ico